# Targeted legacy import — 23 September 2026

Scope: active portal users (email, position, grade, profile pictures), the school organisation chart, and Portfolio Kepakaran. The old Sites portal and its data were left intact. No other historical modules were imported by this operation.

| Component | Source | Production target | Result and verification |
| --- | --- | --- | --- |
| Users | Sites `portal_users`, 62 rows, 61 active | Cloudflare D1 `portal_users` | 60 active emails already existed, one active account was inserted. Total target users: 61 → 62. No pre-existing user ID, email, name, role, status, directory flag, creation timestamp or deletion state changed. 23 empty grades and 11 untouched bootstrap positions were filled. Two existing nonempty position differences were retained, including a manually edited portal-admin title. No duplicate emails or deletions. |
| Organisation chart | Sites active users with `show_org_chart=1` | D1 organisation fields, public `/api/portal-content` | Ten active chart members, positions and order match the source. One target-only chart membership for a source-inactive account was hidden from the chart; its user account was retained. Browser and API readback passed. |
| Profile pictures | 12 Sites `portal_profiles` references and original R2 bytes | Cloudflare R2 `profiles/source-import-20260923-*`, D1 `portal_profiles` | Eleven active-user images imported; the image belonging to a source-inactive account was not attached. All 12 source bytes were backed up locally. Eleven target uploads passed R2 readback SHA-256 and public portal asset-route SHA-256 checks. |
| Portfolio Kepakaran | Four Sites `staff_portfolio` rows, linked `management_materials` and `skas_evidence` | Corresponding production D1 tables; original school Google Drive files retained | Three active records imported with original portfolio/material/evidence IDs, mapped owner ID, dates, featured choices and evidence states (two approved, one pending). A fourth source-deleted record was not resurrected. All three existing Drive file IDs were verified in the school account and their image/PDF links opened successfully through the production portal. |

The import was first rehearsed against an isolated SQLite restoration of the production D1 export and rerun to confirm idempotence. Production D1 was exported before the first write, after the user/chart import, and after the portfolio import. Each export restored with `PRAGMA integrity_check=ok`. The final comparison found all 37 unrelated tables unchanged. The old portal was read-only throughout; no source rows, files, sharing settings or permissions were altered.

Private backups, source metadata, photo checksums and repeatable SQL are stored outside the Git checkout in `../backups/targeted-users-20260923/` (relative to this task's repository parent). The final D1 SQL export SHA-256 is `239648483aa0803d8c086d02da916f89a71662db7231fd84c9209b0c90e7553e`. The backup directory is owner-restricted and must not be committed to Git.

The original Google Drive files remain the storage source for portfolio certificates. This operation did not claim a complete export or reconciliation of every historical Sites table or R2 object. A full old-portal migration would still require the separate source-wide export and reconciliation gate documented elsewhere.

Recovery: keep the original Sites deployment and the three D1 exports. For a targeted data rollback, compare current production writes with `d1-before.sql` first; do not blindly restore the whole database because later writes would be lost. Reverse only the inserted user, filled fields, chart flags, photo references, and the three linked portfolio/material/evidence records after preserving any intervening changes. Uploaded profile objects may be retained until a separately authorized cleanup.
